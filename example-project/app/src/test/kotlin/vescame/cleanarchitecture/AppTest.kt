package vescame.cleanarchitecture

import io.kotest.core.spec.style.ShouldSpec

internal class AppTest : ShouldSpec({

    should("assert test application runs") {
        println("tested should spec")
    }
})
